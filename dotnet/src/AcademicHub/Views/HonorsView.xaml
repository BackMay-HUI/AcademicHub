<UserControl x:Class="AcademicHub.Views.HonorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#x1F3C6;" FontSize="28" Margin="0,0,15,0"/>
                    <TextBlock Text="荣誉档案" FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                </StackPanel>
                <TextBlock Text="记录和管理你的荣誉奖项" FontSize="14"
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="43,5,0,0"/>
            </StackPanel>

            <!-- Stats Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Honors -->
                <Border Grid.Column="0" Background="{DynamicResource CardBrush}"
                        CornerRadius="14" Padding="20" Margin="0,0,12,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="&#x1F3C5;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="荣誉总数" FontSize="14"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding TotalHonors}"
                                   FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource PrimaryBrush}"/>
                    </StackPanel>
                </Border>

                <!-- School Level -->
                <Border Grid.Column="1" Background="{DynamicResource CardBrush}"
                        CornerRadius="14" Padding="20" Margin="6,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="&#x1F3EB;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="校级" FontSize="14"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding SchoolLevelCount}"
                                   FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource ChartColor2}"/>
                    </StackPanel>
                </Border>

                <!-- Province Level -->
                <Border Grid.Column="2" Background="{DynamicResource CardBrush}"
                        CornerRadius="14" Padding="20" Margin="6,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="&#x1F3AD;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="省级" FontSize="14"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ProvinceLevelCount}"
                                   FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource ChartColor3}"/>
                    </StackPanel>
                </Border>

                <!-- National Level -->
                <Border Grid.Column="3" Background="{DynamicResource CardBrush}"
                        CornerRadius="14" Padding="20" Margin="12,0,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="&#x1F451;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="国家级" FontSize="14"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding NationalLevelCount}"
                                   FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource ChartColor1}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Add Honor Form -->
            <Border Grid.Row="2" Background="{DynamicResource CardBrush}"
                    CornerRadius="14" Padding="18" Margin="0,0,0,18">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                             Padding="10,8" Margin="0,0,10,0" VerticalContentAlignment="Center"
                             FontSize="13">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1" CornerRadius="6">
                                                <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="输入荣誉名称..." Foreground="Gray" Padding="10,8"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <ComboBox Grid.Column="1" ItemsSource="{Binding HonorTypes}"
                              SelectedItem="{Binding NewType}"
                              Width="100" Padding="10,8" Margin="0,0,10,0" FontSize="13">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBoxStyle}"/>
                        </ComboBox.Style>
                    </ComboBox>

                    <ComboBox Grid.Column="2" ItemsSource="{Binding HonorLevels}"
                              SelectedItem="{Binding NewLevel}"
                              Width="90" Padding="10,8" Margin="0,0,10,0" FontSize="13">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBoxStyle}"/>
                        </ComboBox.Style>
                    </ComboBox>

                    <TextBox Grid.Column="3" Text="{Binding NewDate, UpdateSourceTrigger=PropertyChanged}"
                             Width="120" Padding="10,8" Margin="0,0,10,0" VerticalContentAlignment="Center"
                             FontSize="13">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1" CornerRadius="6">
                                                <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="日期如2024-01" Foreground="Gray" Padding="10,8"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <Button Grid.Column="4" Command="{Binding AddHonorCommand}"
                            Padding="16,8" Margin="0,0,8,0"
                            Background="{DynamicResource PrimaryBrush}"
                            Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock Text="+" FontSize="14" FontWeight="Bold" Margin="0,0,6,0"/>
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                                    <Setter TargetName="border" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="添加"/>
                    </Button>

                    <Button Grid.Column="5" Command="{Binding DeleteHonorCommand}"
                            Padding="16,8"
                            Background="#E74C3C"
                            Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock Text="&#x2715;" FontSize="12" Margin="0,0,6,0"/>
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                                    <Setter TargetName="border" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="删除"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" Background="{DynamicResource CardBrush}"
                        CornerRadius="14" Padding="18" Margin="0,0,0,18">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,25,0">
                            <TextBlock Text="&#x1F50D;" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="类型筛选:" VerticalAlignment="Center" Margin="0,0,10,0"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <ComboBox ItemsSource="{Binding Types}"
                                      SelectedItem="{Binding SelectedType}"
                                      Width="110" Padding="10,8" FontSize="13">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBoxStyle}"/>
                                </ComboBox.Style>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="级别筛选:" VerticalAlignment="Center" Margin="0,0,10,0"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <ComboBox ItemsSource="{Binding Levels}"
                                      SelectedItem="{Binding SelectedLevel}"
                                      Width="110" Padding="10,8" FontSize="13">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBoxStyle}"/>
                                </ComboBox.Style>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Honors Table -->
                <Border Grid.Row="1" Background="{DynamicResource CardBrush}"
                        CornerRadius="14" Padding="0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.08"/>
                    </Border.Effect>
                    <DataGrid ItemsSource="{Binding Honors}"
                              SelectedItem="{Binding SelectedHonor}"
                              Style="{DynamicResource ModernDataGridStyle}"
                              ColumnHeaderStyle="{DynamicResource ModernDataGridColumnHeaderStyle}"
                              CellStyle="{DynamicResource ModernDataGridCellStyle}"
                              RowStyle="{DynamicResource ModernDataGridRowStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="荣誉名称" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100"/>
                            <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80"/>
                            <DataGridTextColumn Header="获得日期" Binding="{Binding Date}" Width="100"/>
                            <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="200"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
