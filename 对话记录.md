# 对话记录 - AcademicHub 项目开发

---

## 日期: 2026-02-13

### 项目创建
用户希望开发一款本地大学生学业与荣誉管理助手，集成成绩管理、荣誉档案、毕业追踪、保研模拟和Markdown笔记功能，支持深色模式及简历导出。

### 初始实现
- 创建了基于 PyQt5 的桌面应用
- 使用 SQLite 作为本地数据库
- 实现了以下功能模块：
  - 成绩管理
  - 荣誉档案
  - 毕业追踪
  - 保研模拟
  - Markdown 笔记
  - 简历导出
  - 深色模式

---

## 日期: 2026-02-13 (续) - UI和功能优化

### 用户需求
1. 优化学期内容，将学期选项改为大学对应阶段（如大一上、大一下等）
2. 改善UI，界面太小导致部分内容显示不完全
3. 增加绩点内容，调整GPA算法

### 实现内容

#### 1. 学期选项优化
- 文件: `academic_hub/config.py`
- 修改: 将学期列表从 "2024-1", "2024-2" 等改为 "大一上", "大一下", "大二上", "大二下", "大三上", "大三下", "大四上", "大四下"

#### 2. 窗口大小调整
- 文件: `academic_hub/config.py`, `academic_hub/app.py`
- 修改: 将默认窗口大小从 1200x800 调整为 1400x900

#### 3. GPA计算优化
- 采用标准绩点算法
- 支持绩点设置功能，可在标准算法和自定义算法间切换

---

## 日期: 2026-02-13 (续) - 后续优化

### 用户反馈问题
1. UI仍然存在问题，在简历导出选项中明显看不清界面内容
2. 显示有问题，GPA、加权平均分、已获得学分下面对应数据显示错位
3. 需要的自定义绩点不是定义各分数段对应绩点，而是在提交成绩时可以输入绩点

### 实现内容

#### 1. UI尺寸进一步调整
- 文件: `academic_hub/config.py`, `academic_hub/app.py`
- 窗口默认尺寸: 1600x1000
- 最小尺寸: 1200x800
- 导航栏宽度: 240px

#### 2. 统计卡片显示修复
- 文件: `academic_hub/widgets/cards.py`
- 调整卡片大小为 180x110
- 数值居中显示，解决显示错位问题

#### 3. 绩点直接输入功能
- 文件: `academic_hub/database.py`, `academic_hub/widgets/dialogs.py`
- 在成绩表中添加 gpa 字段
- 添加成绩时可输入绩点（选填）
- 如果填写了绩点，则使用输入的绩点计算GPA
- 如果不填，则根据分数自动计算绩点

### GPA 计算逻辑
```
GPA = Σ(绩点 × 学分) / Σ学分

优先级：
1. 优先使用直接输入的绩点
2. 如果未输入绩点，根据配置的算法自动计算
```

### Git 提交记录
1. 初始版本 - AcademicHub 大学生学业与荣誉管理助手
2. 优化学期选项、窗口大小和GPA计算算法
3. 添加绩点计算方式设置，支持标准算法和自定义切换
4. 更新README文档
5. 优化UI尺寸、修复统计卡片显示、新增绩点直接输入功能
